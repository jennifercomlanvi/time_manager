import { getProjects } from "../controllers/project/all";

describe("Get Projects", () => {
  it("should return a list of projects", () => {
    // Écrivez un test pour la fonction getProjects ici
  });
});
